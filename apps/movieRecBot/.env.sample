# Copy this file to ".env" and fill in the non-secret values before running the container.
# API keys and database passwords are provided via Docker secrets, not here.

TELEGRAM_WEBHOOK_URL=
TELEGRAM_BOT_WEBHOOK_PATH=/tg/webhook
SPRING_PROFILES_ACTIVE=postgres

POSTGRES_DB=botdb
POSTGRES_USER=botuser
POSTGRES_PORT=5432

BOT_HTTP_PORT=8080
